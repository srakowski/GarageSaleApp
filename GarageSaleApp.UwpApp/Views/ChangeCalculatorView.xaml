<Page
    x:Class="GarageSaleApp.UwpApp.Views.ChangeCalculatorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GarageSaleApp.UwpApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <TextBlock>Change Calculator</TextBlock>
        <TextBlock>
            Total: $<Run Text="{Binding SaleTotal}"></Run>
        </TextBlock>
        <TextBlock>
            Tender Amount: $<Run Text="{Binding CashTendered}"></Run>
        </TextBlock>
        <Button Command="{Binding EnterDigitCommand}" CommandParameter="9">9</Button>
        <Button Command="{Binding EnterDigitCommand}" CommandParameter="8">8</Button>
        <Button Command="{Binding EnterDigitCommand}" CommandParameter="7">7</Button>
        <Button Command="{Binding EnterDigitCommand}" CommandParameter="6">6</Button>
        <Button Command="{Binding EnterDigitCommand}" CommandParameter="0">0</Button>
    </StackPanel>
</Page>
